/**
 * Loop roll audio as first half forward, then second half reversed, until `stop()` is called.
 * @returns {Promise<(() => void) | null>}
 */
export function startRouletteRollPingPong(url: any): Promise<(() => void) | null>;
//# sourceMappingURL=rouletteWheelRollPlayback.d.ts.map