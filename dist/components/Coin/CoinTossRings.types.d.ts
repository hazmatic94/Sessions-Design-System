import type { HTMLAttributes } from "react";
export type CoinTossRingsProps = HTMLAttributes<HTMLDivElement> & {
    /** Stage width in pixels. Defaults to 400. */
    size?: number;
    /** Coin diameter in pixels — used to spawn particles around the coin. */
    coinSizePx?: number;
};
//# sourceMappingURL=CoinTossRings.types.d.ts.map