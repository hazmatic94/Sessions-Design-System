{
  "installCommand": "npm install",
  "buildCommand": "npm run build && npm run build:showroom-demos",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
